flyway:
  enabled: false
spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:bootexample;MODE=PostgreSQL
  autoconfigure:
    exclude: org.spring
host:
  host-cas: localhost
  host-virkailija: localhost
